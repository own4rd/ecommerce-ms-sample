spring:
  application:
    name: eurekaserver
  security:
    basic:
      enable: true
    user:
      name: guest # TODO Change to env-var
      password: guest # TODO Change to env-var

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false